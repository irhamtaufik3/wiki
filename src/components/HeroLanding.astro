<style>
.hero-custom {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  margin-top: 4rem;
  animation: fadeIn 1.2s ease-in;
}
.hero-custom img {
  width: 160px;
  margin-bottom: 1rem;
  filter: drop-shadow(0 0 20px #00ffe0aa);
  animation: pulse 3s infinite;
}
.hero-title {
  font-size: 3rem;
  font-weight: 700;
  color: #fff;
  margin-bottom: 0.5rem;
  text-shadow: 0 0 10px #00ffe0aa;
}
.hero-tagline {
  font-size: 1.25rem;
  color: #ccc;
  margin-bottom: 2rem;
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes pulse {
  0%, 100% { filter: drop-shadow(0 0 20px #00ffe0aa); }
  50% { filter: drop-shadow(0 0 30px #00fff0); }
}
</style>

<div class="hero-custom">
  <h1 class="hero-title">Welcome to Maouam Nodelab</h1>
  <p class="hero-tagline">Helping you build and run testnet nodes faster 🚀</p>
</div>
